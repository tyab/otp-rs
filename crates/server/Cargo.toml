[package]
name = "otp-server"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "ネイティブ経路サーバ (JVM OTP を置換する高速起動バイナリ)"

[dependencies]
otp-core = { path = "../core" }
otp-engine = { path = "../engine" }
